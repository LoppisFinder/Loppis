import AdminPage from "@/components/AdminPage";

export const metadata = {
  title: "Admin — LoppisFinder",
  robots: { index: false, follow: false },
};

export default function Page() {
  return <AdminPage />;
}
